# Z-dragon 官网 · 交接文档

> 更新日期：2026-09-06 · 当前版本：V2（AI 科技定位版）

## 一、项目概览

| 项目 | 内容 |
|---|---|
| 线上地址 | https://happyliu9527-star.github.io/zdragon/ |
| 仓库 | https://github.com/happyliu9527-star/zdragon （公开仓库，GitHub Pages 托管，main 分支根目录） |
| 本地部署目录 | `F:\download\zdragon-gh-pages\` |
| 活跃设计稿 | `F:\download\V2 Z-dragon.html`（V1 已归档至 `F:\download\archive\`） |
| 技术形态 | 单文件自包含 HTML（内联 CSS/JS）+ `assets/` 两张概念背景图，无框架、无构建、无外部 CDN 依赖 |

## 二、如何本地运行

无需任何环境：双击 `index.html` 即可在浏览器打开（桌面/移动窗口均可正常渲染）。

## 三、如何修改内容

| 想改什么 | 改哪里 |
|---|---|
| 文案（标题/段落/按钮） | `index.html` 直接搜索对应文字替换 |
| 演示数据（信号台列表、评分、走势） | 文件底部 `<script>` 内 `var DATA={...}` 与 `var QDATA=[...]` |
| 品牌色（朱砂点缀 / 墨蓝主色） | 文件顶部 `:root` 中的 `--brand` / `--navy` 等 CSS 变量 |
| 背景概念图 | 替换 `assets/hero-flow.jpg`（浅色首屏背景，建议 1920 宽横图）与 `assets/dragon-neural.jpg`（深色带背景），保持文件名不变即可 |
| 定价 | `PRICE` 对象（`m` 月付 / `y` 年付）与方案功能列表 |
| 常见问题 | `FAQ` 区块每个 `.faq-item` |

## 四、如何重新发布

1. 修改 `F:\download\zdragon-gh-pages\` 下的文件（或改完 `V2 Z-dragon.html` 后复制为该目录的 `index.html`）
2. 在该目录执行：
   ```bash
   git add -A
   git commit -m "更新说明"
   git push
   ```
3. GitHub Pages 会自动构建，约 1-2 分钟后线上生效

说明：本机 gh CLI 已持久化 GitHub 授权（`gh auth login` 凭据存储），推送无需再手动登录；但 `github.com` 需网络可达——国内直连不稳定，推送前请确认代理软件已开启（本机代理端口 7897）。

## 五、待替换素材清单（上线前请核对）

以下均为**占位示例内容**，正式对外前请替换为真实信息：

- [ ] 首屏/全站数据：覆盖标的 5,400+、算法因子 500+、历史数据 10 年+、三大市场（`metrics` 区块与数据底座卡片）
- [ ] 信号台演示数据：15 个中性企业名（华澜科技、天枢算力等）及其评分/走势（均为虚构示例）
- [ ] 识别池表格：8 行标的的规模/盈利质量/成长速率/回报比率数值
- [ ] 策略仿真指标：年化 +18.6%、风险调整比 1.31、最大波动回撤 -12.5%、收益风险比 1.49（示例）
- [ ] 定价：专业版 ¥899/月、年付 ¥749/月（占位价格，未接入任何支付）
- [ ] 联系方式与备案：页脚当前无 ICP 备案号，正式上线需按工信部要求添加
- [ ] 公司主体信息：页脚"关于我们/加入我们"等链接目前指向页内锚点，需要真实页面后替换

## 六、已知问题

1. **国内访问波动**：`*.github.io` 在国内直连时好时坏（最近一次实测直连 200 约 1.0s），重要场合建议绑定自定义域名或接入国内 CDN
2. **单页结构**：关于我们、案例等以版块形式存在于单页内（按需求"保留原有结构"），如需独立子页可后续扩展
3. **无浏览器自动化实测**：验证方式为 HTTP 实测 + DOM/CSS/标签静态检查，未做截图级走查；移动端适配基于 1024/860/640 三断点 CSS
4. **演示数据标注**：图表组件内已保留"示例数据"小字标注，正式替换真实数据后请同步移除

## 七、版本历史

| 版本 | 日期 | 说明 |
|---|---|---|
| V1 | 2026-09-05 | 初版：TIKR 结构模板 + 量化选股定位，已上线 |
| V2 | 2026-09-06 | 全文案重写为"AI 科技公司"定位（清除股市/选股/股票/投资/量化等直白词汇），新增两张概念背景图，演示数据中姓化处理 |

## 八、合规提示

页面已内置"输出仅为数据研究参考，不构成任何决策建议"的保护性声明（FAQ 与页脚）。重新包装文案后，仍建议保留此类声明，并请法务确认宣传用语与实际业务资质一致。
